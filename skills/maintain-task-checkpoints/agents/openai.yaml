interface:
  display_name: "Maintain Task Checkpoints"
  short_description: "Confirm and maintain recoverable task state"
  default_prompt: "Use $maintain-task-checkpoints to assess whether this task needs persistent recovery state, confirm implicit activation, and maintain the checkpoint."
