interface:
  display_name: "Maintain Task Checkpoints"
  short_description: "Persist and recover state for long coding tasks"
  default_prompt: "Use $maintain-task-checkpoints to maintain a recoverable checkpoint for this long-running task."
