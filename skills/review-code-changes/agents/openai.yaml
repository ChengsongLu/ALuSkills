interface:
  display_name: "Review Code Changes"
  short_description: "Review requested changes at proportional depth"
  default_prompt: "Use $review-code-changes to review this change set for concrete risks at proportional depth."
